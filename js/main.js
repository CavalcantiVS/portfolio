var myCarousel = new bootstrap.Carousel(document.getElementById('carouselExampleCaptions'), {
  interval: 2000,  // Define o intervalo para 2 segundos
  wrap: true       // Permite que o carrossel volte ao primeiro slide após o último
});